package main

// CodeBuddy models are discovered per credential by catalog.go. The static
// model method intentionally remains empty so a stale global model list can
// never bypass selected-credential authorization.
