# Flow Control V3

当前功能、开关语义、模型目录、配置失败回退、管理接口和验证步骤统一在 [本地流控当前指南](flow-control.md)。

规则语义仍为 `version: 3`，管理接口仍为 `schema-version: 3`。本次是工程修正，不新增需要迁移的V4，也不引入Codex Identity。

历史实现说明可通过Git历史查看，不应拿旧版本的失败处理、模型别名或采样策略当作当前行为。
